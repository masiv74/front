import React from 'react';

export const BlueBar = ({ text }) => {
    return (
        <div className="blue-bar">
          <span className="glowing-text">{text}</span>
        </div>
      );
    };
